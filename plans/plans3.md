
Sync with resolume comp seems to be failing and detecting everything as OFF

Add envolope/easing to opacity changes that smooths the output. Keep track of actual position and smoothed position. 

When autopilot triggers a new clip on any layer, blink the LED twice for that layer role/group

When applying a scene, broadcast the update with a global update


